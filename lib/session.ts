// Remembers, while the app is open, whether the intro and sign-up have been finished.
// Every fresh start shows the intro again, which suits a portfolio demo.
// Later this can be saved on the device so the intro only shows once.
export const session = {
  onboarded: false,
  name: 'Amin',
};
